#include <iostream>
#include <vector>

using namespace std;

long long sum(std::vector<int> &a){
    long long ans = 0;
    long long a_size = a.size();
    for(int i = 0; i < a_size; i++){
        n_sum += a[i];
    }

    return ans;
}
